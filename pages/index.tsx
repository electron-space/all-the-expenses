import type { NextPage } from 'next';
import { Button, ButtonGroup } from '@chakra-ui/react';
import { VStack } from '@chakra-ui/react';
const Home: NextPage = () => {
	return <div>{/* <Button colorScheme='blue'>Hello</Button> */}</div>;
};

export default Home;
